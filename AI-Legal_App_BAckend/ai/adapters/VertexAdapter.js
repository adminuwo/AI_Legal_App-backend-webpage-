import BaseAIAdapter from './BaseAIAdapter.js';

/**
 * Vertex AI Adapter Skeleton
 */
export class VertexAdapter extends BaseAIAdapter {
  constructor(config = {}) {
    super('VertexAI', config);
  }
}

export default VertexAdapter;
