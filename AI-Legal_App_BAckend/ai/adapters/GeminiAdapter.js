import BaseAIAdapter from './BaseAIAdapter.js';

/**
 * Gemini AI Adapter Skeleton
 */
export class GeminiAdapter extends BaseAIAdapter {
  constructor(config = {}) {
    super('Gemini', config);
  }
}

export default GeminiAdapter;
